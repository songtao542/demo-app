<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aperise.mapper.user">
    <select id="selectUser" resultType="User">
        select id,name,nickname,sex,weight,UNIX_TIMESTAMP(gmt_create) as gmt_create,UNIX_TIMESTAMP(gmt_modify) as gmt_modify,is_deleted from User where id = #{id}
    </select>
</mapper>